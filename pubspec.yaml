name: qa_bug_session
description: >-
  Flutter package for QA BugSession capture and live-backend replay — semantic
  interaction recording, network evidence, and versioned export. TransTRACK
  original; distinct from pub.dev flutter_user_recorder.
version: 1.3.0
homepage: https://github.com/TransTRACK-ID/qa_bug_session
repository: https://github.com/TransTRACK-ID/qa_bug_session
publish_to: none

environment:
  sdk: ^3.13.3
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  archive: ^4.0.7
  crypto: ^3.0.6
  dio: ^5.9.0
  meta: ^1.16.0
  path_provider: ^2.1.5
  screen_recorder: ^0.3.0
  flutter_quick_video_encoder: ^1.7.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
